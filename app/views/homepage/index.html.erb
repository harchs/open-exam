<h2>Students create answers for questions.</h2>
  
<div class="row">
  <div class="span8 welcome-text offset1">
    Open Exam allows students to create questions for other students to answer. 
    Instructors select the best ones to show up on exams. 
  </div>
</div>



<div class="row">
  <br />

  <br />
    
  <div class="span7">
    <P>Already signed up? Find your account(s) by inputting your email below.</P> 
    <form class="navbar-search pull-left">
      <%= form_tag({:controller => "organizations", :action => "index"}, method: :get) do %>
        <%= text_field_tag :query, params[:query], {:class =>"search-query", :placeholder=>"Enter your email"}  %>   
        <span id="search-by-email"><button type="submit" class="btn btn-primary">Search</button> </span>
      <% end %>
    </form>
    <br />
    <br />
    <% users =  User.find_all_by_email(params[:query]) %> 
    <% org_ids = users.map {|user| user.organization_id} %>   
    <% organizations =  org_ids.map {|id| Organization.find_by_id(id).subdomain }%>  

    <% organizations.each do |org| %>
      <a href="http://<%=org%>.openexam.org/login"> <%= Organization.find_by_subdomain(org).name %> </a> <br />
    <% end %>
  </div>

  <div class="span4">
    <P>Create a new organization and join today!</P> 
    <span id="homepage-signup"><h3><%= link_to "Sign Up", new_organization_path, :class => "btn btn-primary" %></h3></span>
  </div>

</div>